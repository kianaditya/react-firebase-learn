# Skeleton built using React + Firebase

Using Firebase as backend for -

* User Sign Up
* User Sign In and Sign Out
* Password Change and Reset

Uses Context API to manage state and Higher Order Components to extract logic away.